node_modules/
dist/
.superpowers/
client/dist-training/
client/dist-live-fast/
client/dist-combined/
client/dist-chatbot/
client/dist-gi/
.dist/
.env
# Ignore all env variants by default (they may hold real secrets).
# Already-tracked blank templates keep working (gitignore doesn't untrack).
# To add a NEW intentional blank template, force it: git add -f <file>
.env.*
.claude/
.worktrees/
*.wav
nul
CLAUDE.md
.env.example
local.env
AGENTS.md
config.toml887
config.toml
touchpoint/

# Understand-Anything tool (cloned repo) + its generated graph/cache
/Understand-Anything/
/.understand-anything/

# Graphify generated knowledge graph (rebuilt locally by git post-commit hook)
graphify-out/

# Local env files
client/.env.combined
